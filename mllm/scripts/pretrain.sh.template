#!/bin/bash

export NAME=$1

deepspeed mllm/llava/train/train_xformers.py \
    --deepspeed mllm/scripts/zero2.json \
    --model_name_or_path meta-llama/Llama-3.1-8B-Instruct \
    --conv_template plain \
    --root_path /abs/path/to/data/ \
    --db_name CT-RATE_train_reports \
    --channels 10 \
    --data_mean -573.8 \
    --data_std 461.3 \
    --vision_tower dino-vit-base-patch14-10 \
    --mm_vision_checkpoint_path /path/to/vision/tower/ \
    --mm_vision_config_path /path/to/backbone/config/ \
    --mm_projector_type attn_pool \
    --tune_mm_mlp_adapter True \
    --mm_vision_select_layer -2 \
    --mm_use_im_start_end False \
    --mm_use_im_patch_token False \
    --bf16 True \
    --log_dir runs/$NAME \
    --output_dir runs/$NAME/checkpoints \
    --num_train_epochs 1 \
    --per_device_train_batch_size 32 \
    --gradient_accumulation_steps 1 \
    --eval_strategy "no" \
    --save_strategy "steps" \
    --save_steps 24000 \
    --save_total_limit 1 \
    --learning_rate 1e-3 \
    --weight_decay 0. \
    --warmup_ratio 0.03 \
    --lr_scheduler_type "cosine" \
    --logging_steps 1 \
    --tf32 True \
    --model_max_length 2048 \
    --gradient_checkpointing True \
    --dataloader_num_workers 4 \
